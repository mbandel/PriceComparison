package com.example.pricecomparison.feature.posters

import androidx.compose.material.Text
import androidx.compose.runtime.Composable

@Composable
fun PostersScreen(productId: Int) {
    Text(text = "productId $productId")
}
